<h1><%= @market.name %></h1>
<p>
  <strong>Address:</strong>
  <%= @market.address %>
</p>
<br>
<p><strong>Vendors at our Market:</strong></p>
<ul>
  <% @vendors.each do |vendor| %>
    <li id="vendor-<%= vendor.id%>"><%= link_to vendor.name, vendor_path(vendor.id) %></li>
  <% end %>